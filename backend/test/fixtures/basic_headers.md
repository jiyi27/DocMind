# Getting Started

Alpha overview explains the ingestion entry point and gives the first slice of context for retrieval.

Beta overlap paragraph keeps enough trailing context so the next chunk can still mention the same transition.

## Installation

Install the service, configure the environment, and verify the loader can read the file before chunking begins.

## Usage

Use the pipeline to split markdown by semantic blocks while preserving useful header breadcrumbs for recall.
