# Oversized

This paragraph is intentionally long so that strict mode can reject it and non strict mode can still split it into smaller pieces for indexing without breaking the whole ingestion flow. This paragraph is intentionally long so that strict mode can reject it and non strict mode can still split it into smaller pieces for indexing without breaking the whole ingestion flow. This paragraph is intentionally long so that strict mode can reject it and non strict mode can still split it into smaller pieces for indexing without breaking the whole ingestion flow.
