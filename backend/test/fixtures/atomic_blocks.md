# Reference

Intro paragraph that gives the surrounding context before the atomic examples appear.

```python
def greet(name: str) -> str:
    message = f"hello {name}"
    print(message)
    return message.upper()
```

> Quoted guidance should stay together.
> The markdown marker should be removed in the final chunk.

| Feature | Value |
| --- | --- |
| Search | Enabled |
| Chunk Size | 400 |
