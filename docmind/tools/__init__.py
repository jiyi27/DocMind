"""Tools registry — future home for agent tools (search, calculator, etc).

Currently contains the Qdrant search tool, which can be used both
as a standalone function and as a LangGraph-compatible @tool.
"""
